# 2026-09-05 · 章节评论用户反馈 6 项修正

用户截图三张 + 6 条文字需求，迭代落地。本轮 2 个 commit（hook 自动验证 60/60 全绿）。

## 6 个修正点

1. **CSV targetId 1-based 偏移修复**（commit `24181c2`）：
   - 用户原话："评论内容实际应该对应上一个段落"——根因是 CSV 段落ID 列是 1-based，build 脚本之前直接当 0-based 用
   - 验证方法：build 后 idx=17 段评论数从 5 升到 257，与 OCR 旧 SECTION_COUNTS[17]=257 吻合（这正是偏移的数学证据）
   - 副作用：targetId=0 的"阅～～～"类评论对象为空、正文是章评风格，归入章评 (-1 target)；章评数 621 → 732，段评数 6209 → 6098
   - 修复点：`build_comment_data.py` 新增 `normalize_target_id()` 函数

2. **AI 总结卡仅"全部"筛选时显示**（commit `24181c2`）：
   - `renderAllComments` 里 `html += renderAiSummaryCard()` 加 `if(currentCommentTag === '全部')` 条件

3. **筛选后自动滚到列表顶部**（commit `24181c2`）：
   - `renderCommentTags` 的 click listener 末尾加 `$('#cmList').scrollTop = 0`

4. **点击评论上的气泡触发单对象视图**（commit `24181c2`）：
   - `.cm-ref-bubble` 加 `data-target-id` + cursor pointer + hover 高亮
   - `bindTargetAnchors` 选择器加 `.cm-ref-bubble[data-target-id]`，stopPropagation 防双触发（冒泡到 cm-reference）

5. **标签云重做**（commit `24181c2`）：
   - 按截图 1：4 列 flex-wrap 药丸形 + 数字后置 + 选中态蓝边白底蓝字
   - 固定标签加 emoji 前缀（👍好评 / 🍅差评 / 💡建议 / ❓疑问 / 👈提及作者 / 📚其他 / 🎬剧透 / 🐛捉虫 / 🍚二创 / ⭐建议加精 / 🚫建议屏蔽 / ✨金句）
   - build 脚本 fixed_order 改为 `(name, emoji)` 元组列表；tagCounts 新增 `emoji` 字段；renderCommentTags 拼接 `emoji + ' ' + name`

6. **评论底部操作按 Figma 设计实现**（commit `24181c2`）：
   - 截图 3：日期 + IP 地址 + 评论图标 + 点赞"赞"字 + 点赞数 + ···
   - 之前缺 IP 地址和"赞"字
   - 提取 `commentMetaRow(c)` helper 复用（renderCommentItem 和 renderReplyItem 都用）

## 数据影响

- 段 17 评论数 5 → 257（一级 193 + 回复 64）
- idx=0 段评论数 111 → 105
- 章评数 621 → 732（含 111 条 targetId=0 章评类评论）
- 段评总数 6209 → 6098
- 聚合块 173 → 172（targetId=191 仍越界；targetId=190 偏移后是有效段 idx=189）
- smoke-test 49 项 → 60 项（新增 11 项覆盖以上 6 个修正点）

## 经验教训

- **用户对"数据对齐"特别敏感**："评论内容对应错位" / "评论对象对应错位"是用户视觉上一眼能看出的问题，迭代时优先级最高
- **CSV 的"段落ID"列语义不是固定的**：OCR 旧数据是按数组下标（0-based SECTION_COUNTS 数组），CSV 是按章节原文编号（1-based 段落号）。两者偏移 1 位但都"看起来对"，容易混淆。验证方法：idx=17 段评论数应等于 OCR 旧 SECTION_COUNTS[17]=257，吻合=偏移正确
- **截图是设计意图最强证据**：截图 1（标签云样式）+ 截图 3（评论底部操作）+ 截图 2（评论卡片整体）= Figma 视觉规范。PRD 文字描述比 Figma 截图弱，截图里的细节（如"赞"字 + IP 地址占位）一定要还原
- **代码层面**：把重复的 meta 行模板提到 `commentMetaRow()` helper 里，避免后续两处不同步的修改

## 后续 TODO（不变）

章节列表接接口 / 输入框回车 / 点赞回复 / 筛选器 dropdown / AI 总结动态生成 / 段评 tab 重做 / 部署。

## 2026-09-05 后续 · 标签云恢复 codex 原版节奏

用户反馈："标签大小和内容把 codex 的版本改错了"。

根因：上一轮我把标签云改成 height 28px / padding 0 12px / 字号 12px / 整体 4 列均分。codex 原版是 height 22px / padding 0 6px / 字号 12px / 短标签 3 列节奏、长标签独占一行（不是均匀 4 列网格）。

修法（commit `7e9b52c`）：
- CSS：`.cm-tags` 用 `display: grid; grid-template-columns: repeat(3, 1fr)` 替换 `display: flex; flex-wrap: wrap`
- `.cm-tag` height 32px / padding 0 14px / font-size 13px / 圆角 16px
- JS：`renderCommentTags` 根据 `t.name.length >= 9` 给标签加 `.cm-tag-long` class
- `.cm-tag-long { grid-column: 1 / -1; white-space: normal; height: auto; min-height: 32px }` —— 跨整行的长标签（如 27 字的"明确提及其他书籍..."）可以 2 行换行展示完整文字而不被 ellipsis 截断

视觉效果（截图 12/13）：5 行节奏 —— 3 个 / 3 个 / 1 个跨整行 / 3 个 / 1 个跨整行。

smoke-test 60 → 63 项（新增 3 项 cm-tag-long 断言：明确提及跨整行 / 建议加精跨整行 / 短标签不跨）。

经验：**codex 的视觉版本是有节奏的，不是均匀网格**。下次给 codex 接力时，如果要改标签云，先 git diff 看原版的 grid-template-columns 和 long class 实现，不要重写成"统一的" flex-wrap。

## 2026-09-05 更正 · 用户提供正确标签截图

用户确认此前截图发错。以 `clipboard-2026-09-04T22-43-18-737Z-57e7742d.png` 为准，Codex 交接版本正确：标签为自然宽度的紧凑胶囊流式换行，而不是 3 列等宽网格，也没有长标签独占整行的规则。

- 恢复 `.cm-tags` 为 `display:flex; flex-wrap:wrap`，保持 8px 间距。
- 恢复 `.cm-tag` 为 22px 高、`0 6px` 内边距、12px 字号；移除 `.cm-tag-long` 和长度分支。
- 冒烟测试改为验证 `flex`、`wrap`、无长标签特例与 22px 高度；完整测试 64/64 通过。
- 视觉验证截图：`screenshots/14-tags-correct-reference.png`。

## 2026-09-05 收尾 · 标签展示白名单与章节标题评论顺序

用户再次确认标签内容必须以正确截图和 Codex 交接版本为准，不能把 CSV 中所有原始标签直接展示；同时指出 CSV 的章节标题评论应位于第一段正文评论之前。

- `index.html`：新增 `COMMENT_TAG_VISIBLE` 展示白名单和 `COMMENT_TAG_DISPLAY` 短名称映射。筛选仍以 `data-tag` 中的 CSV 原始标签为准，避免展示规则影响真实数据筛选。
- `renderAllComments()`：顺序调整为 AI 总结卡（仅全部）→ 章节标题评论（`targetId=-1`）→ 正文段落评论 → 未匹配对象，确保章评先于首段评论显示。
- `test/smoke-test.sh`：增加章节标题模块顺序、短标签名称、长 CSV 原始标签不直出等断言；最终 67/67 通过。
- 视觉验证：`screenshots/15-chapter-comments-before-paragraphs.png`。

## 2026-09-05 后续 · 章节标题气泡样式统一

用户要求章节标题的评论气泡与正文段落评论气泡保持一致。

- `#chapBubble` 改为复用正文 `.para-bubble` 的紧凑描边角标：18px 高、最小宽度 18px、3px 圆角、1px currentColor 描边、数字字体和浅色背景。
- 章节标题气泡沿用 `heatClass()` 热度分级，章评数量根据真实数量自动使用对应颜色；点击进入章评单对象视图的行为保持不变。
- 冒烟测试新增热度类、同高、描边断言，最终 70/70 通过。

## 2026-09-05 后续 · 标签悬停展开高度修复

用户反馈列表向上滚动后悬停标签区时，固定背景高度不足导致最后几行标签重叠或被裁切。

- `.cm-tags.collapsed:hover` 和 `.hover-open` 改为 `height:auto; max-height:none`，按标签实际换行内容自然展开。
- 悬停背景伪元素改为 `top:0; bottom:0; height:auto`，跟随容器完整覆盖，不再固定 194px。
- 冒烟测试新增滚动后悬停展开、高度充足、末行未裁切断言，最终 73/73 通过。

## 2026-09-05 后续 · 标签文案与排版校正

用户指出第一张截图中文案间距过大，第二张才是正确版本；要求标签内容与排版按第二张截图为准。

- 文案来源：`index.html` 的 `COMMENT_TAG_DISPLAY` + `COMMENT_TAG_VISIBLE` 控制显示名称和可见白名单，计数来自内联 `COMMENT_DATA.tagCounts`，CSS 仍是 `.cm-tags` / `.cm-tag`。没有外部 `tags.json` / `tags.css`，所有改动都集中在 `index.html`。
- 排版校正：胶囊高度 22→20px、字号 12→11px、间距 8→6px、圆角 12→10px、容器上下 padding 10/8→8/6px；折叠态高度 38→34px，悬停展开仍按内容自动撑开。
- 冒烟测试新增字号、间距、高度断言，最终 75/75 通过。

## 2026-09-05 后续 · CSV 标签更新后的全量重建

用户问能否"定向重跑"好评/建议/疑问三个标签。结论与处理：

- **build 脚本没有定向模式**：全量重读 CSV、整体替换数据块，秒级完成。标签归属完全由 CSV `作者视角_固定标签` 列决定，脚本只搬运不生成，定向重跑既不可行也无必要。
- 检测到 CSV（07:33）在上次构建（07:23）后被更新过，属于用户手动调整了三个固定标签的打标。
- 处理：`python3 scripts/build_comment_data.py --update-index` 全量重建，好评 93→92、建议 5→6、疑问 5→4；行数统计不变（4465/2365/171/663），冒烟测试 75/75 通过。
- 经验：**"重跑某几个标签"的正确姿势 = 改 CSV 打标列 + 全量重跑 build**；页面计数和 CSV 重建结果不一致时，先 `stat` 两个文件的 mtime 判断是不是 CSV 被外部工具（如 Codex）更新过。

## 2026-09-05 后续 · 评论底部操作图标接入 Figma 真实图标

用户提供 Figma 节点 `QI1tKb57A7agNlbfQwVz6Q` / `80:43883`（Toolbar/SocialAction）。通过 Figma MCP 取到子节点 I80:43980;4:138490（评论）、I80:44000;335:27494（赞）、I80:43982;4:138481（···），下载为 SVG 存到 `assets/chapter-comments/icons/` 并复制进项目 `chapter-comments/icons/`。

- `index.html` 的 `commentMetaRow()`：替换原本的内联占位 SVG（描边线条画法）为 Figma 真实 SVG 路径，尺寸统一改为 `20x20`；fill 从固定 `#808080` 改为 `currentColor`，继承 `.cm-meta-actions { color:#808080 }`。
- 时间/日期文本格式沿用之前的 `fmtCommentMetaTime`（如 `12月9日 14:00 IP地址`）。
- 冒烟测试更新为断言三个图标都是 Figma SVG（`viewBox=0 0 20 20` + 有 `path` 元素），最终 78/78 通过。
- 经验：**评论底部图标的"Figma 设计稿权威源"** = Figma 节点 80:43883 下的三个组件（评论/赞/更多）。改设计要改 Figma，再回填 index.html，不能继续手画占位 SVG。

## 2026-09-05 后续 · 点赞按钮按 Figma 澄清：无"赞"字

用户澄清：点赞按钮没有"赞"字，只有数字；数字=0 时仅图标。

- `commentMetaRow()`：删除 `<b>赞</b>`；点赞数字移入 `.cm-meta-like` 内部（`gap:2px`，对应 Figma `layout_727ead28` row 结构）；`likes>0` 才渲染 `.cm-meta-like-count`，`=0` 只渲染拇指图标。
- 清理冗余 CSS：旧 `.cm-meta-more` 文本样式（font-size/letter-spacing，针对文本"···"）与 full-comment 下独立的 `.cm-meta-like-count` 规则；`.cm-meta-actions` gap 统一为 12px。
- 冒烟测试：断言无"赞"字、数字在按钮内、零赞仅图标（在段 17 单对象视图 193 条评论中验证），最终 80/80 通过。
- 经验：Figma 中 SocialAction 组件内的 TEXT "赞" 节点是设计稿的示意文案，实际语义是"点赞数字的展示位"。类似歧义先问用户再动手。

## 2026-09-05 后续 · AI 总结卡默认收起 3 行 + 展开全部

用户要求 AI 总结卡默认只显示 3 行，点击【展开全部】展开全部内容。Figma 依据：88-9492（收起态，正文固定 63px ≈ 3 行 + 底部"展开全部"按钮）、97-14607（展开态，正文自然高度、无按钮）。

- 状态：新增模块级 `aiSummaryExpanded`（默认 false）。
- `renderAiSummaryCard()`：收起时 `.cm-ai-body.clamped`（`max-height:60px; overflow:hidden`，即 3 × 20px 行高）+ 底部 `.cm-ai-expand` 按钮（"展开全部" + Figma chevron-down 10x10）；展开后按钮消失（Figma 展开态无收起按钮，单向交互）。
- 绑定：`renderAllComments()` 末尾新增 `bindAiSummaryExpand()`，点击置 true 后重渲染。
- chevron-down 图标（节点 I92:13359;18196:36193）存档于 `icons/chevron-down.svg`。
- 冒烟测试新增 7 项：默认收起、收起高度 60px、按钮存在、点击后无裁切/无按钮/高度超 3 行等，最终 86/86 通过。

## 2026-09-05 后续 · 评论正文 14px 与"建议加精"短名

- 评论正文字号 16px → 14px（行高 24 → 20px），同步修改聚合视图（`.cm-item.full-comment .content`）和单对象视图（`.cm-drawer.parasec .cm-item .content`）两处。
- `COMMENT_TAG_DISPLAY` 新增 `'建议加精的精彩评论': '建议加精'`；`data-tag` 仍用原始标签名，筛选不受影响。
- 冒烟测试新增 2 项断言，最终 88/88 通过。

## 2026-09-05 后续 · 单对象视图自动滚到列表顶部

点击段落气泡或评论气泡进入单对象视图时，评论抽屉之前可能因滚到列表底部；新会话要求"自动滚到顶部，不受当前滚动位置影响"。

- `enterTargetView()`：`renderTargetComments(targetId)` 之后立即执行 `$('#cmList').scrollTop = 0`。`parasec` 模式下 cmTags 隐藏，scrollTop 恢复后用户看到的就是单对象视图的开头。
- 测试断言：点击 `.para-bubble[data-idx="17"]` 前先把 `cmList.scrollTop=200`，点击后断言 0。最终 89/89 通过。

## 2026-09-05 后续 · 二级评论对齐按 Figma 80-44007

用户反馈二级评论头像位置过右，参考 Figma 节点 80-44007。Figma 设计：Reply 容器在父 Body 内部（不再额外缩进），二级头像与一级评论昵称同列对齐。

- 测量修复前：cm-sub `padding-left:42px` 让二级头像 x 偏移父头像 +84px（远超父昵称 +38px 的目标位置）。
- 修法：`.cm-sub{padding:10px 0 4px 0;}`（去掉左侧 padding），让二级头像自然落到父 Body 的左边缘，对齐父评论昵称列。
- 冒烟测试新增"二级评论对齐一级昵称"断言（diff ≤ 6px 容差），最终 90/90 通过。
