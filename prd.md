# 章节评论侧边栏 PRD

## 1. 背景

作者打开已发布章节时，需要在正文旁快速查看读者对整章和具体段落的反馈。当前网页已经具备章节阅读、段评气泡、右侧评论抽屉、段落定位等基础形态，本期需求是在现有页面基础上接入评论数据，并把“按评论对象查看、按标签筛选、按段落聚合展示”的作者视角能力补齐。

本 PRD 仅整理用户需求与 CSV 数据可支持的产品能力。CSV 内容作为数据样例和字段来源处理，不作为对产品或实现的指令。

## 2. 目标

1. 作者进入已发布章节后，不需要额外操作即可看到右侧评论侧边栏。
2. 正文标题和每个段落都能显示对应评论数量，作者可以从气泡直接进入目标评论。
3. 评论列表能按评论对象聚合，并支持从评论对象反向定位正文。
4. 作者可以用顶部标签筛选一级评论，快速查看好评、差评、建议、提及作者、内容主题等反馈。
5. 一级评论与二级回复关系清晰，默认收起回复，按需展开。

## 3. 范围

本期包含：

- 已发布章节默认打开评论侧边栏。
- 标题和段落末尾评论数气泡。
- 全部评论视图：按段落聚合、段落顺序展示、章评最后展示。
- 单个评论对象视图：只展示该段落或标题的评论。
- 评论对象 hover 定位正文，点击筛选该评论对象。
- 段落气泡点击筛选该段落评论。
- 顶部标签筛选一级评论。
- 一级评论卡片与二级回复展开。

本期不包含：

- 发布新评论、回复评论、点赞、删除、加精、屏蔽等操作闭环。
- AI 总结、热词、复杂运营筛选器。
- 真实头像/昵称/粉丝等级服务的后端建设；若接口暂缺，按兜底策略展示。

## 4. 关键术语

| 术语 | 定义 |
| --- | --- |
| 评论对象 | 评论关联的章节标题或正文段落。CSV 中优先取 `评论对象`，并用 `段落ID` 关联正文位置。 |
| 章评 | 以章节标题为对象的评论。CSV 中 `段落ID = -1`。 |
| 段评 | 以正文段落为对象的评论。CSV 中 `段落ID >= 0`。 |
| 一级评论 | 评论列表的主评论。CSV 中 `作者视角_评论层级 = 主评论/段评/章评`。 |
| 二级回复 | 一级评论下的回复。CSV 中 `作者视角_评论层级 = 回复`。 |
| 默认排序值 | 评论服务提供的默认展示顺序字段，CSV 中对应 `自定义排序`。当前样例全为 `-1`，表示没有可用人工排序值。 此时按点赞数排序|
| 标签 | 面向作者视角的评论分类，来自 `作者视角_固定标签` 与 `作者视角_内容标签`。 |

## 5. 数据现状

用户提供的 CSV 共 7,493 行评论/回复数据，可识别为：

- 一级评论 4,465 条。
- 二级回复 3,028 条。
- 章评一级评论 540 条。
- 段评一级评论 3,925 条。
- 有一级段评的段落 172 个。
- 固定标签样例：好评、差评、建议、疑问、提及作者、剧透、捉虫、精彩二创、建议加精、建议屏蔽。
- 内容标签样例：这不就是现实/网贷还债太真实、资本修仙/赛博朋克、太癫/太抽象/太离谱、睡太少/不睡觉太狠、面试像找工作/升学太卷、绝育/变性/器官改造太狠、笑死/绷不住。

CSV 当前不包含稳定的评论者头像 URL、评论者昵称、用户等级/身份标签字段。`用户guid` 可作为用户唯一标识；`extend2` 中有 `roleId` 和 `ipLoc`，但不足以还原真实昵称和头像。

## 6. 数据契约

评论接入前需归一化为以下前端数据结构：

| 字段 | 类型 | 来源/说明 |
| --- | --- | --- |
| `id` | string | `评论/回复id`。 |
| `targetId` | number | `段落ID`。`-1` 表示章评；`0` 起表示正文段落索引。 |
| `targetType` | enum | `chapter` 或 `paragraph`。由 `targetId` 推导。 |
| `targetText` | string | 优先使用 `评论对象`，缺失时从章节标题或段落正文回填。 |
| `parentId` | string | `父级回复id`。一级评论为 `0`。 |
| `topCommentId` | string | `顶级评论id（回复的评论id，评论不用到)`，用于把回复挂到一级评论下。 |
| `level` | enum | `main` 或 `reply`。由 `作者视角_评论层级` 推导。 |
| `body` | string | 优先使用 `作者视角_评论正文`，缺失时解析 `评论内容` JSON 的 `context`。 |
| `avatarUrl` | string | 后端用户资料接口补充；缺失时用 `用户guid` 生成默认头像。 |
| `nickname` | string | 后端用户资料接口补充；缺失时显示匿名书友或 `用户guid` 脱敏值。 |
| `userBadge` | string | 后端用户资料接口补充；缺失时不显示。不要用作者视角内容标签冒充用户身份。 |
| `fixedTags` | string[] | `作者视角_固定标签`，按 `；` 或 `;` 拆分。 |
| `contentTags` | string[] | `作者视角_内容标签`，按 `；` 或 `;` 拆分。 |
| `likeCount` | number | `点赞数`。 |
| `replyCount` | number | 一级评论下真实二级回复数量，优先由归一化聚合得到，接口字段可用于校验。 |
| `createdAt` | datetime | `创建时间`。 |
| `sortValue` | number | `自定义排序`。值为空或 `-1` 时视为无人工排序。 |

## 7. 页面入口

### 7.1 默认状态

作者打开已发布章节后：

1. 中间区域展示已发布章节正文。
2. 右侧评论侧边栏默认展开。
3. 侧边栏默认进入“全部评论”视图。
4. 顶部标签默认选中“全部”。
5. 右侧工具栏仍固定在 viewport 最右侧，不被评论侧边栏挤到中间。

### 7.2 非已发布章节

未发布、草稿、审核中章节不在本期核心范围。若沿用同一页面，应默认不打开评论侧边栏，并在入口处置灰或隐藏评论能力。

## 8. 正文评论气泡

### 8.1 展示位置

1. 章节标题作为章评对象，在标题最后 1 个字后显示评论数气泡。
2. 每个正文段落作为段评对象，在段落最后 1 个字后显示评论数气泡。
3. 气泡是内联元素，不能导致正文明显换行错位；长段落自然换行时，气泡跟随最后一行文本。

### 8.2 数值规则

1. 评论数为 `0` 时不显示气泡。
2. 评论数 `1-999` 时显示真实数字。
3. 评论数大于 `999` 时显示 `999+`。
4. 气泡点击区域不得小于当前原型可点击面积。

### 8.3 计数口径

气泡计数默认使用该评论对象下的全部评论数量，包括一级评论和二级回复。若产品后续希望只统计一级评论，需要在接口层增加明确字段并同步修改文案。

### 8.4 热度配色（三档红系渐进）

正文评论气泡（章评 + 段评）与评论侧栏「原文引用行」右侧的计数气泡，共用同一套热度配色，作家凭颜色即可快速感知讨论热度：

| 档位 | 评论数 | 颜色 | 语义 |
|---|---|---|---|
| 无讨论/低热 | < 30 | 灰 `#6F737A` | 无需关注 |
| 活跃 | 30 – 99 | 琥珀 `#C87A16` | 值得浏览 |
| 热点 | ≥ 100 | 品牌红 `#FF3B4F` | 优先处理 |

1. 两处气泡使用同一分级函数与阈值，同一评论对象在正文和侧栏颜色一致。
2. 数字仍是主信息，颜色只是加速扫读的辅助信号。
3. 侧栏计数气泡 hover 仍为品牌红填充 + 白字，不因档位变化。
4. 阈值先固定（MVP）；后续若冷门章节失真，可升级为按本章评论数分布的分位数动态分界。

## 9. 评论卡片

每个一级评论卡片必须包含：

1. 评论对象：章节标题或段落原文摘要。
2. 评论者头像：优先真实头像，缺失时显示默认头像。
3. 评论者昵称：优先真实昵称，缺失时显示guid。
4. 评论者标签：优先真实用户等级/身份标签，缺失时隐藏。
5. 评论正文。
6. 评论回复：有二级回复时显示“展开 n 条回复”按钮，点击后展开回复列表；再次点击收起。

回复展示规则：

1. 回复缩进显示在对应一级评论下。
2. 回复内容优先使用 `作者视角_评论正文`，不展示 `safe_content` 中额外拼接的“回复某某:”前缀，除非产品需要保留回复对象。
3. 回复不单独参与顶部标签筛选结果，但会随其父级一级评论一起展开显示。

### 9.3 评论底部操作栏（Figma 80-43883）

每个一级评论/回复卡片底部操作栏（Toolbar）的按钮序列与间距：

1. 左侧（Left）：评论时间 + IP地址，时间与 IP 之间 gap 4px，文字 Caption 12px/20px `#ADADAD`。
2. 右侧（Right）按 Figma 顺序排列，按钮之间 gap 12px：
   1. **存为灵感便签按钮**（plus-textpart 图标）——hover 显示 tooltip「存为灵感便签」，点击保存并弹出便签（详见 §17）。
   2. 评论按钮（消息气泡图标，title「回复」）。
   3. 点赞按钮（拇指图标）：点赞数 `> 0` 时在图标右侧显示数字（gap 2px，Number12 字体、rgba(20,30,41,0.34)）；`= 0` 时仅图标。
   4. 更多按钮（more 三点图标）。
3. 操作行整体 padding 4px 0，与正文之间保持现有 8px 间距。

## 10. 全部评论视图

### 10.1 默认内容

默认显示当前章节的所有章评和段评。

展示顺序：

1. 段评按正文段落顺序从前到后聚合展示。
2. 章评聚合展示在所有段评之后。
3. 没有评论的段落不展示聚合块。
4. 顶部有1张ai总结卡片

### 10.2 段落聚合块

每个段落聚合块包含：

1. 评论对象：段落原文摘要。
2. 该段评论总数（计数气泡按 §8.4 热度档位着色）。
3. 排序值最前的 3 条一级段评。
4. 如果该段一级段评超过 3 条，显示“查看本段 n 条评论”。

点击“查看本段 n 条评论”后，进入该段落的单对象评论视图。

### 10.3 章评聚合块

章评聚合块展示在最后：

1. 评论对象显示章节标题。
2. 展示排序值最前的 3 条一级章评。
3. 如果一级章评超过 3 条，显示“查看本章 n 条评论”。

点击后进入只看章评的单对象评论视图。

### 10.4 评论排序策略

工具栏提供「默认 / 最热 / 最新」三种排序，对全部评论视图与单个评论对象视图都生效。

#### 10.4.1 默认排序

1. 有效 `sortValue` 越小越靠前。
2. `sortValue` 为空或 `-1` 的评论保持评论服务返回顺序。
3. 同一排序值下保持稳定排序，不因重新渲染而跳动。

CSV 样例中 `自定义排序` 全为 `-1`，因此当前样例的默认展示应保持导入或接口返回顺序。

#### 10.4.2 最热排序（全部评论视图）

1. **聚合块按评论对象总评论数（一级 + 已挂载回复）从高到低排序**，不再保持「章评 → 段落序 → 未匹配」的固定顺序。
2. 每个聚合块内部的一级评论按点赞数降序，同点赞按原始行号稳定排序。
3. 未匹配对象聚合块仍排在所有可匹配块之后。
4. 切换到「最热」后评论列表自动滚动到顶部。
5. 「最热」为作者主动切换的浏览模式，顶部不显示 AI 总结卡。

#### 10.4.3 最新排序（全部评论视图）

1. **不按评论对象聚合**：把当前筛选命中的全部一级评论拍平成单条评论流，按 `createdAt` 从新到旧排序。
2. 单条评论流中不显示「展开此段全部评论」按钮（用户选择「最新」即意味着想按时间线浏览全部评论）。
3. 每条一级评论的二级回复展开行为不变（仍显示「展开 n 条回复」）。
4. 切换到「最新」后评论列表自动滚动到顶部。
5. 「最新」为作者主动切换的浏览模式，顶部不显示 AI 总结卡。
6. **原文引用行**：连续同对象的评论中，首条上方显示与默认聚合块一致的原文引用行（灰底圆角 + 计数气泡，样式同 Figma 80-44095），支持 hover 定位正文与点击进入单对象视图；后续同对象评论省略。

#### 10.4.4 单个评论对象视图的排序

1. 「最热」：一级评论按点赞数从高到低排序。
2. 「最新」：一级评论按 `createdAt` 从新到旧排序。
3. 「默认」：按 10.4.1 规则。

## 11. 标签筛选

### 11.1 标签来源

顶部标签来自一级评论的：

1. `作者视角_固定标签`。
2. `作者视角_内容标签`。

同一评论可命中多个标签。标签字段按 `；` 或 `;` 拆分，去重后展示。

### 11.2 标签展示

1. 顶部始终显示“全部”标签。
2. 默认选中“全部”。
3. 标签文案后显示该标签命中的一级评论数量。
4. 标签顺序建议为：全部、固定标签、内容标签；同组内可按业务预设顺序或数量降序。

#### 11.2.1 标签云折叠与选中可见性

评论列表向下滚动超过阈值后，标签云折叠为固定高度（约 2 行）：

1. 折叠态下鼠标 hover 标签云（或 `hover-open` 态）时恢复全高展开，离开后重新折叠。
2. **选中标签所在行必须始终可见**：若选中标签不在折叠态的可视行内，折叠高度自动滚动定位（`scrollIntoView`）到选中标签所在行，保证用户随时能看到当前生效的筛选。
3. 切换标签或列表滚动引起折叠态变化时，同步执行上述可见性校正。

### 11.3 筛选行为

1. 点击任一标签后，该标签变为选中态。
2. 全部评论视图只展示命中该标签的一级评论。
3. 筛选后仍按评论对象聚合，并保留“段落顺序、章评最后”的排序。
4. 回复不单独作为筛选结果；命中的一级评论有回复时，仍显示“展开 n 条回复”。
5. 点击“全部”恢复展示所有一级评论。

## 12. 单个评论对象视图

进入方式：

1. 点击正文标题后的评论气泡。
2. 点击正文段落后的评论气泡。
3. 点击评论卡片中的评论对象。
4. 点击聚合块的“查看本段 n 条评论”或“查看本章 n 条评论”。

展示规则：

1. 只显示当前评论对象下的所有一级评论。
2. 段落评论按默认排序值展示。
3. 章评按默认排序值展示。
4. 每条一级评论如果有二级回复，显示“展开 n 条回复”按钮。
5. 单对象视图应提供“返回全部评论”入口。
6. 单对象视图默认不展示顶部标签筛选；如后续保留标签筛选，应明确表现为“当前对象内按标签筛选”。
7. **滚动位置保存与恢复（三个入口统一）**：
   - 进入单对象视图前，同步保存评论列表（`#cmList`）与正文区域（`.editor-scroll`）各自的滚动位置。
   - 正文定位到目标段落使用瞬时定位（`behavior:auto`），不使用平滑动画，避免动画残留帧把恢复后的滚动位置拉偏。
   - 点击“返回全部评论”后，评论列表与正文区域**一次性精确恢复**到进入前的滚动位置，不发生二次跳转。
   - 覆盖三个入口：①正文气泡 ②侧栏引用行/计数气泡 ③底部“查看 n 条评论”按钮。

## 13. 评论对象定位

### 13.1 Hover 评论对象

当鼠标 hover 评论卡片中的评论对象时：

1. 中间正文滚动定位到该评论对象所在位置。
2. 段评定位到对应段落，并将段落滚动到中间区域可视范围。
3. 章评定位到章节标题。
4. 定位时给予临时高亮，hover 结束后恢复；不改变当前筛选状态。

#### 13.1.1 Hover 防误触（指针轨迹判定）

为避免鼠标快速扫过评论对象时正文反复滚动跳动，hover 触发滚动定位前需通过防误触判定：

1. 鼠标进入评论对象后启动约 150ms 的观察窗。
2. 观察窗内若鼠标累计位移超过约 20px，判定为「扫过」，取消本次滚动定位。
3. 鼠标稳定停留在评论对象上（观察窗内无大幅位移）才执行正文滚动定位与临时高亮。
4. 鼠标离开评论对象时立即清除未决的观察窗与临时高亮。

### 13.2 点击评论对象

点击评论卡片中的评论对象时：

1. 进入只看该评论对象的评论视图。
2. 中间正文定位并高亮该评论对象。
3. 若当前处于标签筛选状态，点击评论对象后以评论对象筛选为主，标签选中态恢复为“全部”。

## 14. 状态与异常

1. 评论加载中：侧边栏显示加载态，正文气泡可延迟出现。
2. 加载失败：侧边栏显示失败提示和重试入口，正文不显示错误气泡。
3. 评论对象无法匹配正文：该评论进入“未匹配评论对象”分组，展示在章评之后，并记录错误供排查。
4. 评论正文为空：隐藏该条评论，计数以接口清洗后的有效评论为准。
5. 标签为空：评论仍展示在“全部”中，不进入其他标签。
6. 回复找不到父级评论：不展示在主列表，记录为数据异常。

## 15. 实现提示

基于当前 `index.html` 原型，后续实现时建议关注以下改造点：

1. 当前评论列表使用 `SECTION_POOL` 生成演示评论，需要替换为 CSV 或接口归一化后的真实评论数据。
2. 当前 `COMMENT_TAGS` 为静态数组，需要改为从一级评论标签聚合生成。
3. 当前标签点击只更新选中态，没有重新筛选列表，需要补充筛选和重渲染。
4. 当前单段评论视图只展示前 2 条，需要改为展示该对象下全部一级评论。
5. 当前全部评论视图展示 AI 总结和少量演示评论，需要改为按段落聚合展示真实评论。
6. 当前标题章评气泡在评论模式下隐藏，需要按本需求改为标题末尾展示。
7. 当前段评气泡数来自 `SECTION_COUNTS`，真实接入后应由归一化评论数据按评论对象聚合计算或使用后端计数字段。
8. 当前原型已有右侧工具栏贴边约束，实现评论侧边栏时需要继续保持。

## 16. 验收标准

1. 打开已发布章节后，右侧评论侧边栏默认展开，并默认展示全部评论。
2. 标题和所有有评论的段落末尾显示评论数气泡；评论数为 0 的对象不显示气泡；超过 999 显示 `999+`。
3. 点击任一段落气泡后，侧边栏只展示该段落的全部一级评论，并可展开二级回复。
4. 全部评论视图按段落顺序聚合段评，章评排在最后。
5. 全部评论视图中，每个段落默认最多展示 3 条一级评论；超过 3 条时显示“查看本段 n 条评论”。
6. 点击“查看本段 n 条评论”后，进入对应段落评论视图。
7. 评论卡片包含评论对象、头像、昵称、用户标签、正文和回复入口；缺失头像/昵称/用户标签时按兜底策略展示或隐藏。
8. Hover 评论对象时，中间正文滚动到对应段落或标题，并临时高亮；鼠标快速扫过（观察窗内位移超阈值）不触发滚动。
9. 点击评论对象时，侧边栏只展示该评论对象下的评论。
10. 顶部标签默认选中“全部”；点击任一标签后，只展示命中该标签的一级评论，并继续按评论对象聚合。
11. 标签筛选不把二级回复单独列入结果，但命中的一级评论可展开其回复。
12. 页面刷新后默认仍打开评论侧边栏，除非产品另行要求记忆用户关闭状态。
13. 评论输入框占位符文案为「作者有话说」。
14. 评论正文（含回复）渲染后不出现 `[fn=N]` 字样。
15. 全部评论视图选择「最热」：聚合块按评论对象总评论数从高到低排序，列表自动滚动到顶部；对特定标签筛选也生效；不显示 AI 总结卡。
16. 全部评论视图选择「最新」：一级评论按时间从新到旧单条平铺（无聚合块、无「展开此段全部评论」按钮），列表自动滚动到顶部；对特定标签筛选也生效；不显示 AI 总结卡；连续同对象首条评论上方显示与默认一致的原文引用行。
17. 单对象视图选择「最热」按一级评论点赞数降序；选择「最新」按一级评论时间从新到旧。
18. 标签云折叠态下，选中标签所在行始终可见（自动滚动定位）。
19. 评论底部操作栏按 Figma 80-43883 顺序渲染：存为灵感便签 → 评论 → 点赞 → 更多，按钮间距 12px。
20. hover 灵感便签按钮 300ms 后 tooltip 显示「存为灵感便签」；点击后 tooltip 切换为「已保存」并在 1.5s 后消失。
21. 点击灵感便签按钮后窗口左上角出现便签（263×227、固定高度内部滚动、置顶悬浮）；重复点击同条评论不重复创建。
22. 便签内容为「用户{guid}在《没钱修什么仙》第1章 面试的评论」+ 换行 + 评论正文。
23. 多个便签按 16px 步进偏移叠放；便签可拖动到窗口任意位置；点击便签提升到最顶层。

## 17. 灵感便签

### 17.1 入口与提示

1. 入口为评论底部操作栏的「存为灵感便签」按钮（plus-textpart 图标，见 §9.3）。
2. hover 按钮时显示 tooltip「存为灵感便签」（延迟 300ms 出现，移开后消失）。
3. 点击按钮后 tooltip 立即切换为「已保存」，1.5s 后自动消失；点击期间不因鼠标移开而提前隐藏。
4. 同一条评论重复点击只更新 tooltip「已保存」，**不重复创建便签**。

### 17.2 便签窗口（Figma 98-14975）

1. 外观：固定尺寸 263×227，底色 `#FFF6BA`，边框 1px rgba(20,30,41,0.12)，阴影 `0 6px 22px rgba(0,0,0,0.24)`，圆角 8px。
2. 标题栏（高 36px，padding 4px 8px）：
   - 右侧：close-l 关闭按钮（28×28），点击移除便签；MVP 演示版标题栏只有一个关闭按钮在右上角，不设拖动手柄、不实现「新建空白便签」（plus-circle）和「折叠内容区」（content-fold）按钮。
3. 内容区：padding 0 12px 8px，文字 PingFang SC 400 14px/25px、颜色 rgba(20,30,41,0.92)。
4. 内容格式（三段：正文 + 空行 + 署名）：
   ```
   {评论正文}

   ——用户{guid}在《没钱修什么仙》第1章 面试的评论
   ```
   正文在前；中间空一行（`<br><br>`）；破折号「——」开头接用户/作品/章节信息。
5. 高度固定 227px；内容超出高度时在内容区内部滚动查看（overflow-y auto），便签整体尺寸不变。
6. **内容可编辑**：内容区 `contenteditable`，点击正文进入编辑态（显示光标），可直接修改文字、换行；编辑态与浏览态视觉一致（MVP 不做富文本工具栏）。编辑仅保存在会话内存，刷新后丢失（同 §17.5）。拖动与编辑互不干扰：拖动从标题栏触发，点击正文直接定位光标。

### 17.3 位置与层级

1. 便签默认从浏览器窗口左上角打开（position fixed，相对视口）。
2. 多个便签按创建顺序偏移叠放：第 n 个便签的 left/top = 16 × (n mod 10)，避免无限偏移溢出屏幕。
3. 便签始终悬浮在窗口最上方（z-index ≥ 99999）；点击任意便签将其提升到当前最高层级。

### 17.4 拖动与编辑

1. 按住标题栏可拖动便签到窗口任意位置（拖动区域不含关闭按钮与正文编辑区）。
2. 拖动使用 pointerdown/pointermove/pointerup 实现，拖动过程中不触发按钮点击。
3. 拖动时限制便签不会完全拖出可视区域。
4. 点击正文进入编辑（见 §17.2.6）；编辑不会触发拖动。

### 17.5 数据与持久化

1. MVP 阶段便签为会话内存态：刷新页面后清空。
2. 真实产品应将便签内容保存到服务端（按用户维度），本原型不实现。

### 9.1 用户身份徽章（PRD §9.1）

**当前数据现状**：CSV 没有用户身份等级字段（如「盟主」「书生」/ Hug 数量等）。截图里的紫色「盟主」徽章来自起点接口的用户身份数据，本地 CSV 未携带。

**MVP 临时方案**：为 `userGuid` 尾号等于 `56` 的用户 fake 一个「盟主」徽章。徽章紧贴 nickname 右侧渲染，样式按 Figma 盟主组件（SVG，39×24，含六边形 + 渐变背景），CSS `.cm-user-badge` 控制垂直对齐。

**真实数据接入**：当本地数据补充或接入起点接口时，按 `userGuid` 查用户身份表（如盟主/书生/举人、Hug 数量等），按真实身份渲染徽章或去除 fake 逻辑。MVP 临时方案仅用于演示，PRD §16 验收与冒烟测试同步覆盖 fake 徽章的 DOM 与样式。

### 9.1 评论正文中的脚注标记 [fn=N]

CSV 评论文本中存在起点评论体系的脚注引用标记（如 `[fn=21]`，样例数据共 916 条评论携带 `[fn=1]`~`[fn=63]`）：

1. **展示层完全移除** `[fn=N]` 标记，评论文本不显示该字样。
2. 清洗发生在前端渲染层（`commentBodyHtml`），CSV 与内联 `COMMENT_DATA` 保留原始文本，便于后续调整策略（如改为上标展示）而无需重建数据。
3. 回复正文同样适用该规则。

